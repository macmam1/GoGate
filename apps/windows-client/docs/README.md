# Windows Client (Phase 1)

Scope:
- Desktop shell UI (GoGate)
- Connection dashboard + server list + logs
- Performance themes (Lite/Balanced/Rich)
- Reduced motion support
- Integration with core/orchestrator via local API

Packaging target:
- Installer (.exe/.msi in later packaging step)
