# Android Client (Phase 1)

Scope:
- Android UI shell and connection states
- Profile import/select/connect
- Core compatibility with shared profile format
- Performance-first rendering and battery-safe behavior

Distribution target:
- GitHub Releases (APK) in first public beta
